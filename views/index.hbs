<h1>Projects</h1>

{{#each projectArray as |project|}}

	<div class="project">
		<div class="project-name">
			<a href="/project/{{project.name}}">
			{{project.name}}
			</a>
		</div>

	</div>
{{/each}}
